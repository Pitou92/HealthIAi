# Copy to .env and adjust values

# URL of the backend API (default: localhost for dev)
EXPO_PUBLIC_API_URL=http://localhost:8000

# Set to "false" to disable mock mode and hit the real API
# Default is mock mode (true) when this variable is absent
EXPO_PUBLIC_USE_MOCK=true
